FROM docksal/play-with-docksal:pure

CMD \
    fin project create name=wp --choice=4 -y && \
    while true ; do script -q -c "/bin/bash -l" /dev/null ; done
# ... and then put a shell in the foreground, restarting it if it exits
